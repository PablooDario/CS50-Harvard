## Bank 

In the file `bank.py`the user inputs a greeting, if the gretting starts with 'Hello' the bank will return $0, if starts with 'H' the bank will return $20, in other case the bank will return $100

## Test_Bank

This file is to test the previous condtions that are in the `bank.py`file, but are in a different way, so we can handle errors.